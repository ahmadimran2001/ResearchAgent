{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-default",
  "description": "Minimum permissions for the local desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
