# Model card

Research Agent does not train or distribute a custom language model. Chat completions
come from models the user installs in Ollama (Phi-4 Mini by default). Those models keep
their original licenses, weights, and quality claims.

Treat model output as untrusted text. Verify claims against retrieved source records
before using them in research work.
